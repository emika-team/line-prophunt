{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute prophunt-db --local --file=./src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute prophunt-db --remote --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.10",
    "zod": "^3.25.42"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "wrangler": "^4.23.0",
    "typescript": "^5.7.3"
  }
}
