{
  "name": "line-prophunt",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "npm run dev -w packages/api-service",
    "admin": "npm run dev -w packages/admin-ui",
    "build": "npm run build -w packages/api-service && npm run build -w packages/admin-ui"
  }
}
